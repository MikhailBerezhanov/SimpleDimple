# Locations are relative to the exec location
set(RUNTIME_ORIGIN "..")

set(LOGS_DIR "${RUNTIME_ORIGIN}/log")
set(MAIN_LOG_PREFIX "${LOGS_DIR}/main-log")

set(ASSETS_DIR "${RUNTIME_ORIGIN}/assets")
set(ASSETS_IMAGES_DIR "${ASSETS_DIR}/images")